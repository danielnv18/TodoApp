# Copy to .env and adjust values for local development. Do NOT commit the real .env.
SA_PASSWORD=YourStrong!Passw0rd
DB_USER=sa
DB_NAME=TodoDb
DB_PORT=1433
# From host, use localhost; from another container, use the service name "sqlserver".
DB_HOST=localhost
# Optional: full connection string override for ASP.NET Core (double underscore for nesting).
# ConnectionStrings__DefaultConnection=Server=localhost,1433;Database=TodoDb;User Id=sa;Password=YourStrong!Passw0rd;Encrypt=False;TrustServerCertificate=True;
